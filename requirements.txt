# requirements.txt
# Core
flet>=0.21.0

# PDF export (desktop fallback when Android PrintManager not available)
weasyprint>=60.0

# Image processing (APK icon extraction)
Pillow>=10.0

# APK parsing helpers
androguard>=3.4.0       # analyze Dalvik bytecode / manifest
axmlparserpy>=0.3.0     # decode binary XML from APK

# Type checking (dev)
mypy>=1.0
ruff>=0.3.0
